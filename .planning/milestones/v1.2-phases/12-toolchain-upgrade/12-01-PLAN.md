# Plan: Phase 12 — Toolchain Upgrade

**Phase:** 12
**Plan:** 12-01
**Requirements:** TC-04, TC-05, TC-06, TC-07, TC-08, TC-09
**Created:** 2026-04-24

## Goal Summary

Upgrade the Nova CUDA library toolchain from:

- CMake: 3.25 → 4.0+
- C++: 20 → 23
- CUDA: 17 → 20

## Requirements Coverage

| Requirement | Description | Success Criteria |
|-------------|-------------|------------------|
| TC-04 | CMAKE_CUDA_STANDARD upgrade to 20 | CMakeLists.txt updated, builds pass |
| TC-05 | All CUDA 17 code compiles with CUDA 20 | Build succeeds without errors |
| TC-06 | Target CUDA architectures validated | Build targeting 60, 70, 80, 90 |
| TC-07 | cmake_minimum_required updated to 4.0 | CMakeLists.txt updated |
| TC-08 | CMake 3.25+ syntax validated | CMake configuration works |
| TC-09 | CMake generated files regenerated | Clean build succeeds |

## Files to Modify

### CMakeLists.txt (Primary)

```diff
- cmake_minimum_required(VERSION 3.25)
+ cmake_minimum_required(VERSION 4.0)

- set(CMAKE_CXX_STANDARD 20)
+ set(CMAKE_CXX_STANDARD 23)

- set(CMAKE_CUDA_STANDARD 17)
+ set(CMAKE_CUDA_STANDARD 20)
```

### README.md

Update requirements section:

- CUDA Toolkit 12+ → CUDA Toolkit 20+
- CMake 3.25+ → CMake 4.0+
- C++20 → C++23

## Implementation Details

### Step 1: Update CMakeLists.txt

**File:** `CMakeLists.txt` lines 1-19

Change version requirements as shown above.

### Step 2: Update README.md

**File:** `README.md` lines 206-212

Update "Requirements" section.

### Step 3: Verify CI Configuration

**File:** `.github/workflows/ci.yml`

Check if CUDA version needs updating.

## Test Plan

1. Clean build:

   ```bash
   rm -rf build/
   mkdir build && cd build
   cmake -DCMAKE_BUILD_TYPE=Release ..
   cmake --build . -j$(nproc)
   ```

2. Run tests:

   ```bash
   ctest --output-on-failure
   ```

3. Verify test count (should be 418+):

## Success Criteria

1. **CMakeLists.txt updated** — VERSION 4.0, C++23, CUDA 20
2. **Build succeeds** — No compilation errors
3. **All tests pass** — 418+ tests passing
4. **README updated** — Requirements section reflects new versions
5. **Planning documents updated** — STATE.md, ROADMAP.md, REQUIREMENTS.md

## Rollback Strategy

```bash
git checkout HEAD~1 -- CMakeLists.txt README.md
rm -rf build/
```

---

*Plan created: 2026-04-24*
*Ready for execution*
