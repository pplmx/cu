---
phase: 82
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - include/cuda/sparse/krylov.hpp
  - include/cuda/sparse/sparse_ops.hpp
  - include/cuda/sparse/roofline.hpp
autonomous: true
requirements:
  - INT-03

must_haves:
  truths:
    - "User can profile solver operations via NVTX annotations"
    - "NVTX domain 'nova_sparse' is used for all sparse operations"
    - "Annotations are enabled via compile-time flag NOVA_NVTX"
  artifacts:
    - path: "include/cuda/sparse/nvtx_sparse.h"
      provides: "NVTX domain for sparse operations"
      exports: ["SparseNVTXDomain"]
    - path: "include/cuda/sparse/krylov.hpp"
      provides: "Updated with NVTX annotations"
      contains: "NVTX ranges in solver iteration loops"
  key_links:
    - from: "include/cuda/sparse/nvtx_sparse.h"
      to: "include/cuda/benchmark/nvtx.h"
      via: "uses existing NVTX macros"
---

<objective>
Add NVTX annotations with dedicated "nova_sparse" domain to all sparse operations including SpMV, solver iterations, and format conversions. This implements INT-03 (NVTX integration).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@include/cuda/benchmark/nvtx.h
@include/cuda/sparse/krylov.hpp
@include/cuda/sparse/sparse_ops.hpp

# Key patterns from existing NVTX implementation

## Existing NVTX macros (from cuda/benchmark/nvtx.h)

```cpp
#if defined(NOVA_NVTX_ENABLED) && NOVA_NVTX_ENABLED
#define NOVA_NVTX_SCOPED_RANGE(name) \
    cuda::benchmark::nvtx::ScopedRange<> _nova_nvtx_range_(name)
#define NOVA_NVTX_MARK(name) \
    cuda::benchmark::nvtx::mark(name)
#else
#define NOVA_NVTX_SCOPED_RANGE(name) \
    if (false) cuda::benchmark::nvtx::ScopedRange<> _nova_nvtx_range_(name)
#define NOVA_NVTX_MARK(name) \
    do { (void)sizeof(name); } while (false)
#endif
```

## Existing domain definitions

```cpp
struct MemoryDomain {
    static constexpr char const* name{"Nova::Memory"};
};
struct DeviceDomain {
    static constexpr char const* name{"Nova::Device"};
};
struct AlgoDomain {
    static constexpr char const* name{"Nova::Algorithm"};
};
```

## Sparse operations to annotate

1. SpMV kernels (CSR, ELL, SELL, HYB)
2. CG/GMRES/BiCGSTAB iteration loops
3. Format conversions (CSR->ELL, CSR->SELL, CSR->CSC)
4. Roofline analysis operations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SparseNVTXDomain header</name>
  <files>include/cuda/sparse/nvtx_sparse.h</files>
  <action>
Create new header file `include/cuda/sparse/nvtx_sparse.h` with:

1. **SparseNVTXDomain** struct:

   ```cpp
   struct SparseNVTXDomain {
       static constexpr char const* name{"nova_sparse"};
       static constexpr nvtx3::domain_handle_t handle = nvtx3::domain_create(name);
   };
   ```

2. **SparseNVTX** namespace with helper functions:
   - `begin_spmv()` / `end_spmv()` - for SpMV operations
   - `begin_solver_iteration(const char* solver, int iter)` - for solver iterations
   - `begin_format_conversion(const char* from, const char* to)` - for conversions
   - `mark_iteration_count(int count)` - mark final iteration count
   - `mark_convergence(bool converged)` - mark solver convergence status

3. **SparseNVTX macros** (NOVA_SPARSE_NVTX_*):
   - `NOVA_SPARSE_NVTX_SCOPED_SPMV` - scoped range for SpMV
   - `NOVA_SPARSE_NVTX_SCOPED_SOLVER_ITER` - scoped range for single iteration
   - `NOVA_SPARSE_NVTX_MARK_CONVERGENCE` - mark with convergence status
   - `NOVA_SPARSE_NVTX_MARK_ITERATIONS` - mark with iteration count

4. **All macros should be no-ops when NOVA_NVTX_ENABLED is not defined** (following existing pattern)

Include `<nvtx3/nvtx3.hpp>` only when NOVA_NVTX_ENABLED. Use existing cuda/benchmark/nvtx.h pattern for disabled state.

Follow C++20 conventions. Use `if constexpr` where appropriate.
  </action>
  <verify>
grep -c "SparseNVTXDomain" include/cuda/sparse/nvtx_sparse.h
grep -c "nova_sparse" include/cuda/sparse/nvtx_sparse.h
grep -c "scoped_range\|ScopedRange" include/cuda/sparse/nvtx_sparse.h
  </verify>
  <done>SparseNVTXDomain header created with annotations for sparse operations</done>
</task>

<task type="auto">
  <name>Task 2: Add NVTX to sparse_ops.hpp</name>
  <files>include/cuda/sparse/sparse_ops.hpp</files>
  <action>
Update `include/cuda/sparse/sparse_ops.hpp` to add NVTX annotations:

1. **Add include** for nvtx_sparse.h at top:

   ```cpp
   #include "nvtx_sparse.h"
   ```

2. **Annotate SparseOps::spmv(CSR)**:
   - Wrap the SpMV computation loop in `NOVA_SPARSE_NVTX_SCOPED_SPMV`
   - Use message "CSR-SpMV" or similar

3. **Annotate SparseOps::spmv(ELL)**:
   - Add scoped range for ELL-SpMV

4. **Annotate SparseOps::spmv(SELL)**:
   - Add scoped range for SELL-SpMV

5. **Annotate SparseOps::spmm (multiplication)**:
   - Add scoped range for multi-vector SpMM

6. **Mark with matrix properties** (optional payload):
   - `mark_with_payload("SpMV", nnz)` to mark with NNZ as payload

Keep annotations minimal - they should not add significant overhead. Use static scoped objects where possible.

Note: Consider adding payload for iteration count or matrix size when marking.
  </action>
  <verify>
grep -c "NOVA_SPARSE_NVTX\|SparseNVTX" include/cuda/sparse/sparse_ops.hpp
grep -c "scoped_range\|ScopedRange" include/cuda/sparse/sparse_ops.hpp
  </verify>
  <done>SpMV operations annotated with NVTX ranges in nova_sparse domain</done>
</task>

<task type="auto">
  <name>Task 3: Add NVTX to Krylov solvers</name>
  <files>include/cuda/sparse/krylov.hpp</files>
  <action>
Update `include/cuda/sparse/krylov.hpp` to add NVTX annotations:

1. **Add include** for nvtx_sparse.h (if not already included via another path)

2. **Annotate CG solver iteration loop**:
   - Wrap iteration body in scoped range: `NOVA_SPARSE_NVTX_SCOPED_SOLVER_ITER("CG", iter)`
   - Mark convergence: `NOVA_SPARSE_NVTX_MARK_CONVERGENCE(result.converged)`
   - Mark iteration count: `NOVA_SPARSE_NVTX_MARK_ITERATIONS(result.iterations)`

3. **Annotate GMRES solver**:
   - Mark outer restart loop
   - Mark Arnoldi iteration within each restart
   - Mark GMRES-specific events

4. **Annotate BiCGSTAB solver**:
   - Mark BiCGSTAB iteration body
   - Mark alpha/omega computation steps

5. **Add outer-level annotations**:
   - Wrap entire solve() method in scoped range "CG-solve", "GMRES-solve", "BiCGSTAB-solve"

6. **Use payload for performance data**:
   - `mark_with_payload("iteration", iter)` for key iterations
   - `mark_with_payload("residual", static_cast<int64_t>(residual * 1e6))` for residual tracking

Keep annotations lightweight. Focus on marking key phases, not every line.
  </action>
  <verify>
grep -c "NOVA_SPARSE_NVTX\|SparseNVTX" include/cuda/sparse/krylov.hpp
grep -c "scoped_range\|ScopedRange" include/cuda/sparse/krylov.hpp
  </verify>
  <done>Krylov solver iterations annotated with NVTX ranges in nova_sparse domain</done>
</task>

<task type="auto">
  <name>Task 4: Add NVTX to format conversions</name>
  <files>include/cuda/sparse/sparse_matrix.hpp</files>
  <action>
Update `include/cuda/sparse/sparse_matrix.hpp` to add NVTX annotations:

1. **Add include** for nvtx_sparse.h

2. **Annotate SparseMatrixELL::FromCSR()**:
   - Wrap entire conversion in scoped range
   - Mark as "CSR-to-ELL" conversion

3. **Annotate SparseMatrixSELL::FromCSR()**:
   - Wrap conversion in scoped range
   - Mark as "CSR-to-SELL" with slice_height in payload

4. **Annotate SparseMatrixCSC::FromCSR()**:
   - Wrap conversion in scoped range
   - Mark as "CSR-to-CSC" conversion

5. **Annotate FromDense() factory method**:
   - Wrap in scoped range for matrix creation from dense

6. **Add annotations for HYB format** (if HYB exists):
   - Mark partition step
   - Mark ELL and COO conversion steps separately

Use descriptive names: "FormatConversion:CSR->ELL", "FormatConversion:CSR->SELL-32" etc.
  </action>
  <verify>
grep -c "NOVA_SPARSE_NVTX\|SparseNVTX" include/cuda/sparse/sparse_matrix.hpp
grep -c "FromCSR\|FromDense" include/cuda/sparse/sparse_matrix.hpp | wc -l
  </verify>
  <done>Format conversions annotated with NVTX ranges in nova_sparse domain</done>
</task>

</tasks>

<verification>

## Phase 82 Plan 02 verification

1. **SparseNVTXDomain exists:**
   - `grep -c "SparseNVTXDomain" include/cuda/sparse/nvtx_sparse.h` returns 1

2. **nova_sparse domain defined:**
   - `grep "nova_sparse" include/cuda/sparse/nvtx_sparse.h` returns the domain name

3. **SpMV annotated:**
   - `grep -c "NOVA_SPARSE_NVTX" include/cuda/sparse/sparse_ops.hpp` returns 3+

4. **Solver loops annotated:**
   - `grep -c "NOVA_SPARSE_NVTX" include/cuda/sparse/krylov.hpp` returns 5+

5. **Format conversions annotated:**
   - `grep -c "NOVA_SPARSE_NVTX" include/cuda/sparse/sparse_matrix.hpp` returns 2+

6. **Code compiles without NVTX:**
   - Create test file that includes sparse headers with NVTX disabled
   - Verify it compiles without errors
</verification>

<success_criteria>

- NVTX domain "nova_sparse" created and used for all sparse operations
- SpMV operations annotated with scoped ranges
- Solver iteration loops annotated with NVTX
- Format conversions annotated
- No performance overhead when NOVA_NVTX_ENABLED=0 (macros are no-ops)
</success_criteria>

<output>
After completion, create `.planning/phases/82/82-02-SUMMARY.md`
</output>
