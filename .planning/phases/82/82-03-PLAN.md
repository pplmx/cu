---
phase: 82
plan: 03
type: execute
wave: 2
depends_on:
  - 82-01
  - 82-02
files_modified:
  - tests/sparse/integration_test.cpp
  - tests/sparse/benchmark_test.cpp
autonomous: true
requirements:
  - INT-01
  - INT-02

must_haves:
  truths:
    - "User can run end-to-end tests validating sparse formats, Krylov solvers, and Roofline analysis"
    - "User can run benchmarks comparing sparse format performance and solver convergence rates"
    - "Test output includes pass/fail status and performance metrics"
  artifacts:
    - path: "tests/sparse/integration_test.cpp"
      provides: "E2E integration tests"
      contains: "CSR->ELL->SpMV->CG->Roofline"
    - path: "tests/sparse/benchmark_test.cpp"
      provides: "Performance benchmarks"
      contains: "SpMV and solver benchmarks"
  key_links:
    - from: "tests/sparse/integration_test.cpp"
      to: "include/cuda/sparse/krylov.hpp"
      via: "uses solvers with workspace"
    - from: "tests/sparse/benchmark_test.cpp"
      to: "include/cuda/sparse/roofline.hpp"
      via: "uses RooflineAnalyzer"
---

<objective>
Create end-to-end integration tests and performance benchmarks. This implements INT-01 (E2E tests) and INT-02 (benchmarks) by testing the complete pipeline from sparse matrix creation through solver execution with Roofline analysis.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@tests/sparse/krylov_test.cpp
@tests/algo/spmv_test.cpp
@include/cuda/sparse/sparse_matrix.hpp
@include/cuda/sparse/krylov.hpp
@include/cuda/sparse/roofline.hpp

# Test patterns from existing codebase

## From krylov_test.cpp

```cpp
template<typename T>
bool test_cg_trivial() {
    std::vector<T> dense = {T{4}, T{1}, T{1}, T{3}};
    auto csr = SparseMatrixCSR<T>::FromDense(dense.data(), 2, 2);
    // ... solver call and verification
}

int main() {
    return nova::sparse::test::KrylovSolverTest<double>::run_all();
}
```

## From spmv_test.cpp (for benchmark patterns)

- Generate test matrices
- Time operations with cudaEvent_t
- Report performance metrics

## Test matrix types

- Laplacian 1D/2D (tridiagonal)
- Random sparse matrices
- Matrices from real applications (if available)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E integration test</name>
  <files>tests/sparse/integration_test.cpp</files>
  <action>
Create new test file `tests/sparse/integration_test.cpp` with:

1. **SparseIntegrationTest<T>** class with these test cases:

   a. **test_csr_ell_spmv_pipeline()**:
   - Create dense Laplacian matrix
   - Convert to CSR
   - Convert CSR to ELL
   - Run SpMV on both formats
   - Compare results (should match within tolerance)
   - Report: "CSR->ELL->SpMV pipeline: PASSED/FAILED"

   b. **test_csr_sell_spmv_pipeline()**:
   - Same pattern for SELL format
   - Test with different slice heights (16, 32, 64)

   c. **test_solver_workspace_reuse()** (per D-01):
   - Create SolverWorkspace
   - First solve with CG
   - Second solve with CG (same workspace)
   - Verify both solutions are correct
   - Verify workspace was reused (timing improvement expected)
   - Report iteration counts and timing

   d. **test_cg_with_roofline()**:
   - Create Laplacian matrix
   - Run CG solver
   - Create RooflineAnalyzer
   - Measure SpMV performance
   - Classify as compute/memory bound
   - Report classification

   e. **test_full_pipeline_csr_to_solution()** (per D-02):
   - Create CSR from dense
   - Convert to ELL format
   - Run SpMV with CG solver
   - Validate solution residual
   - Run Roofline analysis
   - Export results to JSON
   - Report all steps succeeded

   f. **test_format_conversion_correctness()** (per D-03):
   - Create test matrix
   - Convert: CSR -> ELL, CSR -> SELL, CSR -> CSC
   - Run SpMV on each format
   - Verify all results match CSR baseline
   - Report conversion correctness

2. **Test matrix generation helpers**:
   - `generate_laplacian_1d(int n)` - tridiagonal matrix
   - `generate_laplacian_2d(int n)` - 2D grid Laplacian
   - `generate_random_sparse(int n, double sparsity)` - random sparse matrix

3. **Output format**:
   - Print test name before running
   - Print "PASSED" or "FAILED: <reason>" after
   - Print summary at end with counts
   - Print timing information for workspace reuse test

Follow existing test style from krylov_test.cpp. Use `nova::sparse::test` namespace.
  </action>
  <verify>
grep -c "test_csr_ell_spmv_pipeline\|test_csr_sell_spmv_pipeline" tests/sparse/integration_test.cpp
grep -c "SolverWorkspace" tests/sparse/integration_test.cpp
grep -c "RooflineAnalyzer\|to_json" tests/sparse/integration_test.cpp
  </verify>
  <done>Integration tests cover CSR->ELL->SpMV->CG->Roofline pipeline end-to-end</done>
</task>

<task type="auto">
  <name>Task 2: Create performance benchmark test</name>
  <files>tests/sparse/benchmark_test.cpp</files>
  <action>
Create new test file `tests/sparse/benchmark_test.cpp` with:

1. **SparseBenchmark<T>** class with these benchmark cases:

   a. **benchmark_spmv_formats()** (per D-04):
   - Generate test matrices of various sizes (1K, 10K, 100K elements)
   - Generate matrices with various sparsity patterns (uniform, clustered)
   - Time SpMV for: CSR, ELL, SELL (slice heights: 16, 32, 64)
   - Use cudaEvent_t for accurate timing
   - Run multiple iterations (10+) and take median
   - Report: GFLOPs/s, bandwidth (GB/s), format comparison table
   - Output example:

        ```text
        SpMV Benchmark (n=10000, nnz=50000):
        Format    GFLOPs/s    Bandwidth GB/s
        CSR       125.3       45.2
        ELL       132.1       47.8
        SELL-32   130.5       47.2
        ```

   b. **benchmark_solver_convergence()** (per D-05):
   - Test CG, GMRES, BiCGSTAB on same matrix
   - Record iterations to convergence
   - Record final residual
   - Record solve time
   - Report comparison table:

        ```text
        Solver Convergence Comparison (Laplacian 100x100):
        Solver      Iterations    Time (ms)    Final Residual
        CG          47            2.3          1.2e-8
        GMRES(50)   23            3.1          9.8e-9
        BiCGSTAB    31            2.8          8.5e-9
        ```

   c. **benchmark_workspace_reuse()** (per D-01):
   - Time 100 consecutive CG solves WITHOUT workspace reuse
   - Time 100 consecutive CG solves WITH workspace reuse
   - Compare total time and average time per solve
   - Report speedup factor

   d. **benchmark_matrix_sizes()**:
   - Test SpMV and solver performance across matrix sizes
   - Sizes: 100, 1K, 10K, 100K rows
   - Generate appropriate sparsity pattern per size
   - Report scaling behavior

2. **Output format**:
   - Print benchmark header with matrix properties
   - Print formatted table of results
   - Print summary with best performer per category

3. **Benchmark configuration**:
   - Default iterations: 10 warmup, 100 measured
   - Default matrix sparsity: 5% (95% zeros)
   - Configurable via environment variables if needed

Follow patterns from `tests/algo/spmv_test.cpp` and `tests/benchmark/benchmark_test.cpp`.
  </action>
  <verify>
grep -c "benchmark_spmv_formats\|benchmark_solver_convergence" tests/sparse/benchmark_test.cpp
grep -c "GFLOPs\|GB/s" tests/sparse/benchmark_test.cpp
grep -c "cudaEvent_t\|elapsed_ms" tests/sparse/benchmark_test.cpp
  </verify>
  <done>Benchmarks compare SpMV format performance and solver convergence rates</done>
</task>

<task type="auto">
  <name>Task 3: Create benchmark runner and documentation</name>
  <files>tests/sparse/benchmark_runner.cpp</files>
  <action>
Create optional benchmark runner `tests/sparse/benchmark_runner.cpp`:

1. **Main function** that:
   - Accepts command line arguments: `--spmv`, `--solver`, `--workspace`, `--sizes`
   - Runs selected benchmarks
   - Outputs results in machine-readable format (JSON option: `--json`)
   - Supports `--iterations=N` to set benchmark iterations

2. **Example usage**:

   ```bash
   # Run all benchmarks
   ./benchmark_runner

   # Run only SpMV benchmarks
   ./benchmark_runner --spmv

   # Run with 1000 iterations
   ./benchmark_runner --iterations=1000

   # Output JSON
   ./benchmark_runner --json > results.json
   ```

3. **Also create a simple README in comments** at top of benchmark_test.cpp explaining:
   - How to compile: `g++ -std=c++20 -I include tests/sparse/benchmark_test.cpp -o benchmark_test`
   - How to run: `./benchmark_test`
   - What the output means

Note: This is a standalone executable, not a gtest test.
  </action>
  <verify>
ls -la tests/sparse/benchmark_runner.cpp 2>/dev/null && echo "File exists" || echo "File not needed (optional)"
grep -c "int main\|getopt\|cxxopts" tests/sparse/benchmark_runner.cpp
  </verify>
  <done>Optional benchmark runner executable with command-line options</done>
</task>

</tasks>

<verification>

## Phase 82 Plan 03 verification

1. **Integration tests exist:**
   - `grep -c "test_csr_ell_spmv_pipeline\|test_solver_workspace_reuse" tests/sparse/integration_test.cpp` returns 2+

2. **Benchmarks exist:**
   - `grep -c "benchmark_spmv_formats\|benchmark_solver_convergence" tests/sparse/benchmark_test.cpp` returns 2+

3. **Tests use workspace:**
   - `grep -c "SolverWorkspace" tests/sparse/integration_test.cpp` returns 1+

4. **Benchmarks use timing:**
   - `grep -c "cudaEvent_t" tests/sparse/benchmark_test.cpp` returns 2+

5. **Tests compile:**
   - `g++ -std=c++20 -I include -c tests/sparse/integration_test.cpp -o /tmp/integration_test.o`
   - `g++ -std=c++20 -I include -c tests/sparse/benchmark_test.cpp -o /tmp/benchmark_test.o`

6. **Output format includes metrics:**
   - `grep "GFLOPs\|Iterations\|Bandwidth" tests/sparse/benchmark_test.cpp`
</verification>

<success_criteria>

- E2E integration test validates CSR->ELL->SpMV->CG->Roofline pipeline
- Benchmark compares SpMV performance across CSR, ELL, SELL formats
- Benchmark compares solver convergence across CG, GMRES, BiCGSTAB
- All tests compile without errors
- Output includes clear metrics and pass/fail status
</success_criteria>

<output>
After completion, create `.planning/phases/82/82-03-SUMMARY.md`
</output>
